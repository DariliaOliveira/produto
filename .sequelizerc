const path = require('path');
const { database} = require('./config/config');

module.exports = {
    'config': database,
    'models-path': path.resolve('.models'),
    'seeders-patch': path.resolve('.seeders'),
    'migrations-path': path.resolve('.migrations')
};